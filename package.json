{
  "name": "sistema-camisa-tsubasa",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev:backend": "pnpm --filter camisa-backend dev",
    "docker:up": "docker compose up -d --build",
    "docker:down": "docker compose down"
  }
}
